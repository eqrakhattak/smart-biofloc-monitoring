import 'package:flutter/material.dart';

class PhPage extends StatelessWidget {
  const PhPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Text('Hello Ph');
  }
}
