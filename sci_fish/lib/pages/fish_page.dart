import 'package:flutter/material.dart';

class FishPage extends StatelessWidget {
  const FishPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Text('Hello Fish');
  }
}
