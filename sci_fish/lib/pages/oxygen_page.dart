import 'package:flutter/material.dart';

class OxygenPage extends StatelessWidget {
  const OxygenPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Text('Hello Oxygen');
  }
}
